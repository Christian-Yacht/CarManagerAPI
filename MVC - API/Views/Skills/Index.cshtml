@model IEnumerable<APIassignment2.Models.Skill>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SkillTitle)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SkillTitle)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SkillTitle">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SkillTitle">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SkillTitle">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
